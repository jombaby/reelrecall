import { NextRequest, NextResponse } from "next/server";

export const maxDuration = 300;

type Source = "Instagram" | "Facebook";
type VideoInput = { url: string; title: string; notes: string; source: Source };
type RecipeResult = { available:boolean; title:string; ingredients:string[]; steps:string[]; notes:string; servings:string; prepTime:string };
type ActorRow = Record<string, unknown>;

function actorId(value:string){return value.replace("/","~")}
async function runActor(actor:string,input:unknown){const token=process.env.APIFY_TOKEN;if(!token)throw new Error("APIFY_TOKEN is not configured");const response=await fetch(`https://api.apify.com/v2/acts/${actorId(actor)}/run-sync-get-dataset-items?token=${encodeURIComponent(token)}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(input),signal:AbortSignal.timeout(150000),cache:"no-store"});if(!response.ok)throw new Error(`Reel retrieval failed (${response.status})`);const data=await response.json() as unknown;return Array.isArray(data)?data as ActorRow[]:[]}
function text(value:unknown){return typeof value==="string"?value.trim():""}
function firstText(row:ActorRow,keys:string[]){for(const key of keys){const value=text(row[key]);if(value)return value}return""}
function firstUrl(row:ActorRow,keys:string[]){for(const key of keys){const value=text(row[key]);if(/^https?:\/\//i.test(value))return value}return""}

async function instagramEvidence(video:VideoInput){const actor=process.env.APIFY_INSTAGRAM_VIDEO_ACTOR||"automation-lab/instagram-reel-ocr-hooks";const rows=await runActor(actor,{startUrls:[{url:video.url}],maxItems:1,frameIntervalSeconds:2,hookWindowSeconds:6,ocrLanguage:"eng",includeTranscript:true,maxVideoDurationSeconds:180,proxyConfiguration:{useApifyProxy:false}});const row=rows[0]||{};const transcript=firstText(row,["transcript"]),onScreenText=firstText(row,["onScreenText"]),caption=firstText(row,["caption","description"]),thumbnail=firstUrl(row,["thumbnailUrl","displayUrl","imageUrl"]);return{caption,transcript,onScreenText,thumbnail,evidence:[transcript?"Spoken narration":"",onScreenText?"On-screen text from sampled video frames":"",caption?"Instagram reel caption":"",thumbnail?"Reel preview image":""].filter(Boolean)}}

async function facebookEvidence(video:VideoInput){const actor=process.env.APIFY_FACEBOOK_VIDEO_ACTOR||"apivault_labs/facebook-reels-video-scraper";const rows=await runActor(actor,{startUrls:[video.url],maxResults:1,maxCostUsd:.05,downloadMp4:true,includeTranscript:true,proxyCountry:"US",maxConcurrency:1,timeout:90,maxRetries:2});const row=rows[0]||{};let transcript=firstText(row,["transcript","transcriptText","transcriptSummary"]);const caption=firstText(row,["caption","description","title"]),thumbnail=firstUrl(row,["thumbnailUrl","thumbnail","imageUrl"]);if(!transcript){try{const transcriptActor=process.env.APIFY_FACEBOOK_TRANSCRIPT_ACTOR||"automation-lab/facebook-video-transcript-extractor";const transcriptRows=await runActor(transcriptActor,{videoUrls:[video.url],maxItems:1,language:"auto",includeWordTimestamps:false,maxVideoDurationSeconds:180});transcript=firstText(transcriptRows[0]||{},["transcript","text"])}catch{}}return{caption,transcript,onScreenText:"",thumbnail,evidence:[transcript?"Spoken narration":"",caption?"Facebook reel caption":"",thumbnail?"Reel preview image":""].filter(Boolean)}}

export async function POST(request:NextRequest){if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"OPENAI_API_KEY is not configured"},{status:503});if(!process.env.APIFY_TOKEN)return NextResponse.json({error:"APIFY_TOKEN is not configured in Vercel"},{status:503});try{const body=await request.json() as {video?:VideoInput},video=body.video;if(!video?.url||!video.title||!["Facebook","Instagram"].includes(video.source))return NextResponse.json({error:"A Facebook or Instagram reel is required"},{status:400});const media=video.source==="Instagram"?await instagramEvidence(video):await facebookEvidence(video);const evidenceText=[video.notes?`Saved notes:\n${video.notes}`:"",media.caption?`Reel caption:\n${media.caption}`:"",media.transcript?`Spoken narration transcript:\n${media.transcript}`:"",media.onScreenText?`Text read from sampled video frames:\n${media.onScreenText}`:""].filter(Boolean).join("\n\n").slice(0,24000);if(!evidenceText.trim())return NextResponse.json({recipe:null,message:"The public reel could be opened, but no narration, caption, or readable on-screen text was available for recipe analysis."});const content:Array<Record<string,unknown>>=[{type:"input_text",text:JSON.stringify({source:video.source,title:video.title,url:video.url,extractedEvidence:evidenceText})}];if(media.thumbnail)content.push({type:"input_image",image_url:media.thumbnail,detail:"low"});const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",reasoning:{effort:"none"},max_output_tokens:1600,input:[{role:"system",content:[{type:"input_text",text:"Create a cooking recipe only from evidence extracted from the selected reel itself: narration, on-screen text/OCR, caption, and preview image. Do not invent exact quantities, temperatures, cooking times, or ingredients. If an ingredient or action is clear but its amount is absent, use 'amount not specified'. Preserve the sequence of preparation steps. Set available=false only when the evidence is genuinely insufficient to produce a useful recipe with at least one ingredient and one preparation or cooking step."}]},{role:"user",content}],text:{format:{type:"json_schema",name:"reel_recipe_analysis",strict:true,schema:{type:"object",additionalProperties:false,properties:{available:{type:"boolean"},title:{type:"string"},ingredients:{type:"array",items:{type:"string"}},steps:{type:"array",items:{type:"string"}},notes:{type:"string"},servings:{type:"string"},prepTime:{type:"string"}},required:["available","title","ingredients","steps","notes","servings","prepTime"]}}}}),signal:AbortSignal.timeout(45000)});if(!response.ok)throw new Error(`AI recipe analysis failed (${response.status})`);const data=await response.json() as {output?:Array<{content?:Array<{type?:string;text?:string}>}>},outputText=data.output?.flatMap(item=>item.content||[]).find(item=>item.type==="output_text")?.text;if(!outputText)throw new Error("AI returned no recipe result");const result=JSON.parse(outputText) as RecipeResult;if(!result.available||!result.ingredients.length||!result.steps.length)return NextResponse.json({recipe:null,message:"AI analyzed the reel but could not identify enough reliable recipe detail."});return NextResponse.json({recipe:{title:result.title.trim()||video.title,ingredients:result.ingredients.map(x=>x.trim()).filter(Boolean).slice(0,50),steps:result.steps.map(x=>x.trim()).filter(Boolean).slice(0,40),notes:result.notes.trim(),servings:result.servings.trim(),prepTime:result.prepTime.trim(),extractedAt:new Date().toISOString(),source:"video",evidence:media.evidence}})}catch(error){const message=error instanceof Error?error.message:"Could not analyze reel";return NextResponse.json({error:message},{status:500})}}
