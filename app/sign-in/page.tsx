"use client";
import Image from "next/image";
import { FormEvent,useState } from "react";
export default function SignIn(){const[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError("");const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(response.ok){const requested=new URLSearchParams(location.search).get("returnTo"),safeReturnTo=requested?.startsWith("/")&&!requested.startsWith("//")?requested:"/";location.href=safeReturnTo}else{setError("That password is not correct.");setBusy(false)}}return <main className="sign-in"><section><a className="brand dark" href="/"><Image className="brand-logo" src="/icons/reelreplay-mark.png" alt="" width={42} height={42} priority/><strong>ReelRecall</strong></a><p className="kicker">PRIVATE LIBRARY</p><h1>Welcome back.</h1><p>Your videos, categories, and archive are protected.</p><form onSubmit={submit}><label>Password<input type="password" autoFocus required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error?<p className="form-error">{error}</p>:null}<button className="primary" disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form></section></main>}
